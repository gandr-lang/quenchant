# Changelog

All notable changes to quenchant are documented here.

## Unreleased

### Unreleased Documentation

- *(repo)* Adopt shared changelog guidance
- *(repo)* Adopt empty-root guidance
- *(agents)* Adopt shared fast CI pattern
- *(agents)* Record measured CI adoption

### Unreleased Continuous Integration

- *(ci)* Run cargo-deny gate
- *(ci)* Port image-backed parallel gates
- *(ci)* Include GitHub CLI in the tool image

### Unreleased Configuration

- *(config)* Enforce dependency policy

## 0.0.0 - 2026-09-10

The `quenchant` umbrella retains the specification facade and adds `quenchant::arith`, `quenchant::shape`, crate-root shape macros, and optional `quenchant::gates`; one dependency covers the publishable family.

The six packages enter the looser new-version bucket after the staged first publication pass. Every in-family requirement uses the exact new version.

The root README now leads with the workspace shape, and the package READMEs say exactly which layer they expose. The repository has one explanation path instead of a product slogan followed by a second architecture guide.

### 0.0.0 Features

- *(repo)* Seed the quenchant workspace

### 0.0.0 Maintenance

- *(repo)* Prepare the 0.0.0-rc.0 publication head
- *(repo)* Prepare the 0.0.0 publication head

<!-- Generated by git-cliff; edit release tags and commit messages instead. -->
